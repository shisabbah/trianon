# Site Next.js export statique → site/out (généré par [build] avant deploy / versions upload)
name = "trianon"
compatibility_date = "2024-09-23"

[build]
command = "npm run build"

[assets]
directory = "./site/out"

[observability]
enabled = false
