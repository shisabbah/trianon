{
  "name": "trianon",
  "private": true,
  "description": "Le site Next.js vit dans site/ (npm install && npm run build s'y font dans ce dossier).",
  "scripts": {
    "build": "npm ci --prefix site && npm run build --prefix site && node scripts/prepare-cloudflare-deploy.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.97.0"
  }
}
